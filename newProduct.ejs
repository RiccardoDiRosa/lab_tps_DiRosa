<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>nuovo prodotto di rosa</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary: #8e44ad;
      --secondary: #9b59b6;
      --bg-dark: #121212;
      --bg-card: #1f1f1f;
      --text-light: #f0f0f0;
      --text-muted: #bbbbbb;
    }

    body {
      background-color: var(--bg-dark);
      color: var(--text-light);
      font-family: 'Poppins', sans-serif;
    }

    .title {
      font-size: 2.5rem;
      font-weight: 700;
      background: linear-gradient(to right, var(--primary), var(--secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .card {
      background-color: var(--bg-card);
      border: 1px solid rgba(255,255,255,0.05);
    }

    .form-control {
      background-color: #2a2a2a;
      border: 1px solid rgba(255,255,255,0.1);
      color: var(--text-light);
    }

    .form-control:focus {
      border-color: var(--primary);
      box-shadow: 0 0 0 0.2rem rgba(142, 68, 173, 0.25);
    }

    .btn-primary {
      background: linear-gradient(to right, var(--primary), var(--secondary));
      border: none;
    }

    .btn-primary:hover {
      opacity: 0.9;
    }

    .btn-outline-light {
      color: var(--text-light);
      border-color: #444;
    }

    .btn-outline-light:hover {
      background-color: #444;
    }

    footer {
      color: var(--text-muted);
      border-top: 1px solid rgba(255,255,255,0.05);
      margin-top: 5rem;
      padding-top: 2rem;
    }

    .social-icons a {
      color: var(--text-muted);
      margin: 0 0.5rem;
    }

    .social-icons a:hover {
      color: var(--primary);
    }
  </style>
</head>
<body>

  <div class="container py-5">
    <div class="text-center mb-5">
      <h1 class="title">nuovo prodotto</h1>
      <p class="text-muted">Inserisci i dati del nuovo prodotto</p>
    </div>

    <div class="card shadow-lg p-4">
      <form action="/products" method="POST">
        <div class="mb-4">
          <label for="name" class="form-label"><i class="fas fa-tag me-2"></i>Nome Prodotto</label>
          <input type="text" id="name" name="name" class="form-control" placeholder="Inserisci il nome" required>
        </div>

        <div class="mb-4">
          <label for="description" class="form-label"><i class="fas fa-align-left me-2"></i>Descrizione</label>
          <textarea id="description" name="description" class="form-control" rows="4" placeholder="Inserisci una descrizione dettagliata" required></textarea>
        </div>

        <div class="mb-4">
          <label for="price" class="form-label"><i class="fas fa-euro-sign me-2"></i>Prezzo (€)</label>
          <input type="number" id="price" name="price" class="form-control" step="0.01" min="0" placeholder="Es. 49.99" required>
        </div>

        <div class="d-flex flex-wrap gap-3">
          <button type="submit" class="btn btn-primary">
            <i class="fas fa-plus-circle me-1"></i> aggiungi prodotto
          </button>
          <a href="/products" class="btn btn-outline-light">
            <i class="fas fa-arrow-left me-1"></i> torna ai Prodotti
          </a>
        </div>
      </form>
    </div>
  </div>

  <footer class="text-center">
    <div class="container">
      <p class="mb-2">&copy; Negozio. Tutti i diritti riservati.</p>
      <div class="social-icons">
        <a href="#"><i class="fab fa-facebook-f fa-lg"></i></a>
        <a href="#"><i class="fab fa-instagram fa-lg"></i></a>
        <a href="#"><i class="fab fa-twitter fa-lg"></i></a>
      </div>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
